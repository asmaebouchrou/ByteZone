doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Reegistro 

        h1 Registro 
        form(action="post" onclick="validarContrasenna()")
            input(type="text" placeholder="Nombre completo" required)
            br
            input(type="email" placeholder="Correo electrónico" required)
            br
            input(type="password" placeholder="Contraseña" id="contrasena"  required minlength="5")
            br
            input(type="password" placeholder="Repetir contraseña" id="contrasenaRepetida"  required minlength="5")
            br
            button(type="submit" ) Registrarse
    body 

    script 
        fuction validarContrasenna(){
            let contrasena = document.getElementById('contrasena').value;
            let contrasenaRepetida = document.getElementById('contrasenaRepetida').value;
            if (!(contrasena===contrasenaRepetida)){
                alert("Las contraseñas no coinciden, introducelas de nuevo");
                //Para que no se envie el formulario
                return flase;
            }

        }